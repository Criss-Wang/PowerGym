# Core dependencies
numpy>=1.21.0
numba>=0.62.1
pandas>=2.3.1  # Required for PandaPower network merging (fixes NAType issue)
gymnasium>=0.28.1
pandapower>=2.14.0

# Multi-agent RL dependencies
ray[rllib]>=2.40.0  # Ray 2.40+ / 3.0 compatible
torch>=2.0.0

# Optimization (optional but recommended)
pyscipopt>=4.0.0

# XML parsing (required by pandapower)
lxml>=4.9.0

# Visualization (optional)
matplotlib>=3.5.0

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
ruff>=0.1.0
black>=22.0.0

# Logging (optional)
wandb>=0.15.0
tensorboard>=2.13.0
